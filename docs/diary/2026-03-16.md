# Diary — 2026-03-16

## Summary
Login page logo update, merge conflict resolution, TypeScript build error fixes, and Railway project linking.

## Q&A Log

**Q:** Connect to Railway lifememorywall
**A:** Found Railway CLI installed. Linked project to `lifememorywall` (production environment) using `railway link --project lifememorywall`.

**Q:** Put the actual app logo on the login page (concentric circles from loading screen, not the iOS app icon)
**A:** Replaced the placeholder Heart icon with the concentric circles SVG matching the `LoadingFallback` component in App.tsx. Sized up to 64x64px with proper padding (top-8, left-8). Removed "My Inner Circle" text beside it.

**Q:** Pull from git, stash local changes, merge, and push
**A:** Stashed local changes, pulled 5 updated files from remote, resolved merge conflict in `SpaceSelector.tsx` (merged imports: added `Lock`, `Unlock`, `UserPlus`; kept `allSpaces` naming + notification variables), committed and pushed.

**Q:** Fix TypeScript build errors (4 issues)
**A:** Fixed all 4 errors:
1. Added `import { vi } from 'vitest'` to `src/__tests__/setup.ts`
2. Added `'canvas'` to `newType` union type in `MemoryDetail.tsx`
3. Removed invalid `memory.canvasData` reference in `MemoryDetailC.tsx` (not on `Memory` type)
4. Added `canvasData` property to cover slide object in `slideshowSlides` array for type consistency

**Q:** Run existing test cases
**A:** All 26 tests across 4 test files passed.

**Q:** Test scenarios for icon feature (emoji/image)
**A:** Generated 27 test scenarios covering display, add, edit, type switching, image upload, persistence, and edge cases — text only, no code changes.

**Q:** Are you logging diary every day?
**A:** No — missed entries for March 13-15. Only had entries for March 11 and 12. Created today's entry.

**Q:** Create CLAUDE.md with project instructions
**A:** Created `CLAUDE.md` at project root with diary logging, project info, git push policy, pre-push checklist, testing, code conventions, and theme rules.

**Q:** Add rules to CLAUDE.md — no push without confirmation, test maintenance, UI theming, TypeScript checks, security, performance, accessibility, backend rules
**A:** Added all rules to CLAUDE.md: git push policy, pre-push checklist, test maintenance, code quality, security, performance, accessibility, backend-specific, and UI theming requirements.

**Q:** Implement DOMPurify sanitization to prevent XSS and dangerous HTML injection
**A:** Implemented across both repos:
- **Frontend**: Created `src/utils/sanitize.ts` with `sanitizeHtml()` (allows safe formatting tags) and `sanitizeText()` (strips all HTML). Updated all 5 components using `dangerouslySetInnerHTML` to sanitize content first. Added 11 sanitization test cases.
- **Backend**: Replaced weak regex-based `stripHtml()` in `src/middleware/sanitize.ts` with DOMPurify (`isomorphic-dompurify`). Rich text fields now allow only safe formatting tags; all other fields strip ALL HTML. Updated and added backend sanitization tests.
- Regenerated Prisma client after schema changes.

## Files Changed

| File | Change |
|------|--------|
| `src/components/LoginPage.tsx` | Replaced Heart icon placeholder with concentric circles SVG logo, sized to 64x64px, added padding |
| `src/components/SpaceSelector.tsx` | Resolved merge conflict — merged imports, kept `allSpaces` + notification variables |
| `src/__tests__/setup.ts` | Added `import { vi } from 'vitest'` to fix TS error |
| `src/components/MemoryDetail.tsx` | Added `'canvas'` to `newType` state union type, added `sanitizeHtml` to all `dangerouslySetInnerHTML` |
| `src/components/MemoryDetailA.tsx` | Added `sanitizeHtml` to all `dangerouslySetInnerHTML` |
| `src/components/MemoryDetailB.tsx` | Added `sanitizeHtml` to all `dangerouslySetInnerHTML` |
| `src/components/MemoryDetailC.tsx` | Fixed TS errors, added `sanitizeHtml` to all `dangerouslySetInnerHTML` |
| `src/components/MomentEditor.tsx` | Added `sanitizeHtml` to `dangerouslySetInnerHTML` |
| `src/utils/sanitize.ts` | **New** — DOMPurify-based sanitization utility (sanitizeHtml, sanitizeText) |
| `src/__tests__/sanitize.test.ts` | **New** — 11 test cases for HTML sanitization |
| `CLAUDE.md` | **New** — Project instructions with all rules |
| `public/logo.png` | Copied iOS app icon (unused now, can be removed) |
| **Backend** `src/middleware/sanitize.ts` | Replaced regex stripHtml with DOMPurify, rich text fields sanitized with safe tag whitelist |
| **Backend** `src/__tests__/sanitize.test.ts` | Updated tests for DOMPurify behavior, added event handler stripping test |
| **Backend** `src/generated/prisma/` | Regenerated Prisma client for titleStyle field |
