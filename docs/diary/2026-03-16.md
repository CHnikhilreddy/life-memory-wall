# Diary — 2026-03-16

## Summary
Login page logo update, merge conflict resolution, TypeScript build error fixes, and Railway project linking.

## Q&A Log

**Q:** Connect to Railway lifememorywall
**A:** Found Railway CLI installed. Linked project to `lifememorywall` (production environment) using `railway link --project lifememorywall`.

**Q:** Put the actual app logo on the login page (concentric circles from loading screen, not the iOS app icon)
**A:** Replaced the placeholder Heart icon with the concentric circles SVG matching the `LoadingFallback` component in App.tsx. Sized up to 64x64px with proper padding (top-8, left-8). Removed "My Inner Circle" text beside it.

**Q:** Pull from git, stash local changes, merge, and push
**A:** Stashed local changes, pulled 5 updated files from remote, resolved merge conflict in `SpaceSelector.tsx` (merged imports: added `Lock`, `Unlock`, `UserPlus`; kept `allSpaces` naming + notification variables), committed and pushed.

**Q:** Fix TypeScript build errors (4 issues)
**A:** Fixed all 4 errors:
1. Added `import { vi } from 'vitest'` to `src/__tests__/setup.ts`
2. Added `'canvas'` to `newType` union type in `MemoryDetail.tsx`
3. Removed invalid `memory.canvasData` reference in `MemoryDetailC.tsx` (not on `Memory` type)
4. Added `canvasData` property to cover slide object in `slideshowSlides` array for type consistency

**Q:** Run existing test cases
**A:** All 26 tests across 4 test files passed.

**Q:** Test scenarios for icon feature (emoji/image)
**A:** Generated 27 test scenarios covering display, add, edit, type switching, image upload, persistence, and edge cases — text only, no code changes.

**Q:** Are you logging diary every day?
**A:** No — missed entries for March 13-15. Only had entries for March 11 and 12. Created today's entry.

**Q:** Fix moments text style/size changes not saving
**A:** Found bug: backend PUT handler for substories only extracted `{ date, type, title, content, caption, photos }` from req.body — `textStyle` and `titleStyle` were never extracted, never saved, never returned. Also `titleStyle` column was missing from the Prisma schema entirely. Fixed by: (1) adding `titleStyle String?` to SubStory model in schema.prisma, (2) running `prisma db push` + `prisma generate`, (3) updating GET/POST/PUT handlers to extract, JSON.stringify, and return both style fields, (4) updating `formatMemory` in db.ts to parse and include them.
**Q:** Create CLAUDE.md with project instructions
**A:** Created `CLAUDE.md` at project root with diary logging, project info, git push policy, pre-push checklist, testing, code conventions, and theme rules.

**Q:** Add rules to CLAUDE.md — no push without confirmation, test maintenance, UI theming, TypeScript checks, security, performance, accessibility, backend rules
**A:** Added all rules to CLAUDE.md: git push policy, pre-push checklist, test maintenance, code quality, security, performance, accessibility, backend-specific, and UI theming requirements.

**Q:** Implement DOMPurify sanitization to prevent XSS and dangerous HTML injection
**A:** Implemented across both repos:
- **Frontend**: Created `src/utils/sanitize.ts` with `sanitizeHtml()` (allows safe formatting tags) and `sanitizeText()` (strips all HTML). Updated all 5 components using `dangerouslySetInnerHTML` to sanitize content first. Added 11 sanitization test cases.
- **Backend**: Replaced weak regex-based `stripHtml()` in `src/middleware/sanitize.ts` with DOMPurify (`isomorphic-dompurify`). Rich text fields now allow only safe formatting tags; all other fields strip ALL HTML. Updated and added backend sanitization tests.
- Regenerated Prisma client after schema changes.

## Files Changed

| File | Change |
|------|--------|
| `src/components/LoginPage.tsx` | Replaced Heart icon placeholder with concentric circles SVG logo, sized to 64x64px, added padding |
| `src/components/SpaceSelector.tsx` | Resolved merge conflict — merged imports, kept `allSpaces` + notification variables |
| `src/__tests__/setup.ts` | Added `import { vi } from 'vitest'` to fix TS error |
| `src/components/MemoryDetail.tsx` | Added `'canvas'` to `newType` state union type, added `sanitizeHtml` to all `dangerouslySetInnerHTML` |
| `src/components/MemoryDetailA.tsx` | Added `sanitizeHtml` to all `dangerouslySetInnerHTML` |
| `src/components/MemoryDetailB.tsx` | Added `sanitizeHtml` to all `dangerouslySetInnerHTML` |
| `src/components/MemoryDetailC.tsx` | Fixed TS errors, added `sanitizeHtml` to all `dangerouslySetInnerHTML` |
| `src/components/MomentEditor.tsx` | Added `sanitizeHtml` to `dangerouslySetInnerHTML` |
| `src/utils/sanitize.ts` | **New** — DOMPurify-based sanitization utility (sanitizeHtml, sanitizeText) |
| `src/__tests__/sanitize.test.ts` | **New** — 11 test cases for HTML sanitization |
| `CLAUDE.md` | **New** — Project instructions with all rules |
| `public/logo.png` | Copied iOS app icon (unused now, can be removed) |
| *(backend)* `prisma/schema.prisma` | Added `titleStyle String?` to SubStory model |
| *(backend)* `src/db.ts` | `formatMemory` now parses and returns `textStyle` + `titleStyle` for substories |
| *(backend)* `src/routes/memories.ts` | GET/POST/PUT substory handlers now save and return `textStyle` + `titleStyle` |
| **Backend** `src/middleware/sanitize.ts` | Replaced regex stripHtml with DOMPurify, rich text fields sanitized with safe tag whitelist |
| **Backend** `src/__tests__/sanitize.test.ts` | Updated tests for DOMPurify behavior, added event handler stripping test |
| **Backend** `src/generated/prisma/` | Regenerated Prisma client for titleStyle field |
| `capacitor.config.ts` | Added `iosScheme: 'https'` for WKWebView font/encoding fix |
| `ios/App/App/Info.plist` | Added NSCameraUsageDescription, NSPhotoLibraryUsageDescription, NSPhotoLibraryAddUsageDescription |
| `src/components/FloatingNav.tsx` | Added `fixed-safe-bottom` class for home indicator |
| `src/components/MemoryDetailC.tsx` | Redesigned mobile header, compact cards, smaller floating buttons, safe area handling |
| `src/components/MobileLayout.tsx` | Simplified to only apply `safe-bottom`, removed `safe-top` (headers handle individually) |
| `src/components/RichTextEditor.tsx` | Fixed emoji picker touch with `onPointerDown`, removed font hacks |
| `src/components/SpaceSelector.tsx` | Compact profile dropdown, fix hide-spaces bar positioning, `bottom-safe` on buttons |
| `src/components/Timeline.tsx` | Mobile width optimization, compact members popover, safe area header |
| `src/index.css` | Added native safe area CSS classes, warm body background `#f5ece3` |
| `src/components/SpaceSelector.tsx` | Added `md:` breakpoints to profile dropdown (width, header padding, avatar, menu items, icons, sign-out) |
| `src/components/Timeline.tsx` | Added `md:` breakpoints to MemoryMembersPanel and SpaceMembersPanel (width, padding, invite code) |
| `src/components/MemoryDetailC.tsx` | Added `md:` breakpoints to floating buttons (container position, Relive/New moment sizing) |
| `tailwind.config.js` | Cleaned font stacks (removed emoji font debugging hacks) |

**Q:** iOS mobile app — full optimization pass
**A:** Comprehensive iOS mobile fixes across 10 files:
- Fixed emoji picker touch events (`onPointerDown` for iOS WKWebView)
- Added `iosScheme: 'https'` to Capacitor config (fixes Google Fonts/Telugu in WKWebView)
- Added CSS safe area classes for notch/home indicator (`.sticky-safe`, `.pt-safe`, `.fixed-safe-top`, `.fixed-safe-bottom`, `.bottom-safe`)
- Redesigned mobile moment detail header (combined back button + members + title + tabs in one compact header)
- Maximized mobile content width (reduced paddings, moved timeline line to edge, increased card width)
- Compacted profile dropdown (smaller icons, tighter spacing, narrower width)
- Compacted members popover (w-72→w-56, w-60→w-48)
- Made Relive/New moment buttons smaller and repositioned lower
- Fixed hide-spaces selection bar getting cut off (switched from center-translate to left/right positioning)
- Added camera/photo library/save permissions to iOS Info.plist (`NSCameraUsageDescription`, `NSPhotoLibraryUsageDescription`, `NSPhotoLibraryAddUsageDescription`)
- Added warm fallback background `#f5ece3` to html/body for iOS overscroll areas
- Cleaned up font stacks in tailwind.config.js (removed emoji font hacks from simulator debugging)

**Q:** Fix desktop regressions from mobile optimization (cramped profile dropdown, small popovers, tiny floating buttons)
**A:** Added Tailwind `md:` responsive breakpoints to restore comfortable desktop sizing while keeping compact mobile styles intact. Changes across 3 files: SpaceSelector.tsx (profile dropdown width, header padding, avatar size, menu item spacing/icons, sign-out section), Timeline.tsx (MemoryMembersPanel and SpaceMembersPanel widths/padding, invite code sizing), MemoryDetailC.tsx (floating button container position/gap, Relive button sizing, New moment button sizing). All checks pass: tsc, vitest (37/37), build.

**Q:** Moments not loading in production (infinite loading), works locally
**A:** Root cause: `titleStyle` column was missing from production DB, and `textStyle` was type `String` instead of `Json`. Schema had been updated but `prisma db push` was never run against prod. Fixed by running direct SQL: `ALTER TABLE "SubStory" ADD COLUMN "titleStyle" JSONB` and `ALTER TABLE "SubStory" ALTER COLUMN "textStyle" TYPE JSONB`. No redeployment needed — DB fix was immediate.
